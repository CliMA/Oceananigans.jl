
function communicating_buffer_allocation!(fields)

    @apply_regionally buffer = FieldBoundaryBuffers(ordinary_fields)

    # Rebuild the field with the new boundaries
    for (idx, field) in enumerate(ordinary_fields)

    end
end