env:
  JULIA_VERSION: "1.12.4"
  JULIA_VERSION_ENZYME: "1.10.10"
  BUILDKITE_HOME: "/var/lib/buildkite-agent"
  JULIA_DEPOT_PATH: "$BUILDKITE_HOME/.julia-oceananigans"
  JULIA_PKG_SERVER_REGISTRY_PREFERENCE: eager
  XLA_REACTANT_GPU_PREALLOCATE: false
  OCEANANIGANS_DIR: "$HOME/Oceananigans.jl-$BUILDKITE_BUILD_NUMBER"

agents:
  queue: "Oceananigans"
with:
  coverage: false

steps:
  - label: "🏕️ initialize general environment"
    key: "init"
    agents:
      queue: "Oceananigans-nautilus"
    env:
      TEST_GROUP: "init"
    command: |
      juliaup add $JULIA_VERSION

      mkdir -p "$OCEANANIGANS_DIR"
      rsync -a . "$OCEANANIGANS_DIR/"
      cd "$OCEANANIGANS_DIR"

      julia +$JULIA_VERSION -O0 --color=yes --project -e 'using Pkg; Pkg.test()'
      # After we have instantiated the general environment, move manifest to the
      # version-specific filename, not to clash with the manifest for Enzyme.
      mv -v Manifest.toml Manifest-v$(echo "${JULIA_VERSION}" | cut -d. -f1-2).toml
    retry:
      automatic:
        - exit_status: 1
          limit: 1

  - label: "🏕️ initialize enzyme environment"
    key: "init_enzyme"
    depends_on: "init"
    agents:
      queue: "Oceananigans-nautilus"
    env:
      TEST_GROUP: "init"
    command: |
      juliaup add $JULIA_VERSION_ENZYME
      cd "$OCEANANIGANS_DIR"

      julia +$JULIA_VERSION_ENZYME -O0 --color=yes --project -e 'using Pkg; Pkg.test()'
      # After we have instantiated the Enzyme environment, move manifest to the
      # version-specific filename, not to clash with the general manifest.
      # mv -v Manifest.toml Manifest-v$(echo "${JULIA_VERSION_ENZYME}" | cut -d. -f1-2).toml
    retry:
      automatic:
        - exit_status: 1
          limit: 1

  - label: "🦉 documentation"
    depends_on: "init"
    agents:
      queue: "Oceananigans-nautilus"
    env:
      CUDA_VISIBLE_DEVICES: "0" # GPU for docs
    command: |
      cd "$OCEANANIGANS_DIR"
      julia +$JULIA_VERSION --color=yes --project=docs/ -e \
        'using Pkg; Pkg.instantiate()'
      julia +$JULIA_VERSION --color=yes --project=docs/ docs/make.jl

  - label: "{{ matrix.architecture }} - {{ matrix.group }} tests"
    key: "tests"
    depends_on: "init"
    agents:
      queue: "Oceananigans-nautilus"
    command: |
      # Add matrix-specific environment variables
      if [[ "{{ matrix.architecture }}" == "CPU" ]]; then
        export CUDA_VISIBLE_DEVICES="-1"
        export TEST_ARCHITECTURE="CPU"
      else
        export CUDA_VISIBLE_DEVICES="0"
        export TEST_ARCHITECTURE="GPU"
      fi

      # Strip emoji for environment variable
      group="{{ matrix.group }}"
      export TEST_GROUP="\${group#* }"
      echo $TEST_GROUP

      # Run tests (but don't exit immediately so we can upload coverage even if tests fail)
      set +e
      julia +$JULIA_VERSION -O0 --color=yes --project --code-coverage=user -e 'using Pkg; Pkg.test(coverage="user")'
      test_status=$$?
      set -e

      # Sanity check: did we actually produce coverage?
      echo "PWD:"; pwd
      echo "Coverage files produced:" `find . -type f -name "*.cov" | wc -l`
      find . -type f -name "*.cov" | head -n 20 || true

      group_slug="{{ matrix.group }}"
      group_slug="$${group_slug//[^A-Za-z0-9_.-]/_}"
      tarball="coverage-${BUILDKITE_BUILD_NUMBER}-${BUILDKITE_JOB_ID}-{{ matrix.architecture }}-$${group_slug}.tgz"


      find . -type f -name "*.cov" > cov_list.txt
      echo "Coverage files found:" `wc -l < cov_list.txt`

      tar -czf "$$tarball" -T cov_list.txt
      ls -lh "$$tarball"

      buildkite-agent artifact upload "$$tarball"

      # Preserve the original test result for Buildkite
      exit "$${test_status}"

    matrix:
      setup:
        architecture:
          - "CPU"
          - "GPU"
        group:
          - "🦖 poisson_solvers_2"
          - "🐇 unit"
          - "👻 abstract_operations"
          - "🕊 poisson_solvers_1"
          - "🦤 general_solvers"
          - "🎣 turbulence_closures"
          - "🦀 time_stepping_1"
          - "🦈 time_stepping_2"
          - "🦟 time_stepping_3"
          - "🐫 nonhydrostatic_regression"
          - "🐙 hydrostatic_free_surface"
          - "🫐 tripolar_grid"
          - "🥑 vertical_coordinate"
          - "🙈 hydrostatic_regression"
          - "🦢 shallow_water"
          - "🐳 simulation"
          - "🍂 lagrangian_particles"
          - "🧅 multi_region"
          - "🦧 scripts"
          - "🍱 xesmf"

    retry:
      automatic:
        - exit_status: 1
          limit: 1

  - label: "{{ matrix.architecture }} - {{ matrix.group }} tests"
    key: "tests_enzyme_reactant"
    depends_on: "init_enzyme"
    agents:
      queue: "Oceananigans-nautilus"
    command: |
      # Add matrix-specific environment variables
      if [[ "{{ matrix.architecture }}" == "CPU" ]]; then
        export CUDA_VISIBLE_DEVICES="-1"
        export TEST_ARCHITECTURE="CPU"
      else
        export CUDA_VISIBLE_DEVICES="0"
        export TEST_ARCHITECTURE="GPU"
      fi

      # Strip emoji for environment variable
      group="{{ matrix.group }}"
      export TEST_GROUP="\${group#* }"
      echo $TEST_GROUP
    
      # Installing version 0.2.210 of Reactant since tests seem to fail with version 0.2.211
      # See issue https://github.com/CliMA/Oceananigans.jl/issues/5249
      julia +$JULIA_VERSION_ENZYME -O0 --color=yes --project -e 'using Pkg;  Pkg.add(Pkg.PackageSpec(name="Reactant", version="0.2.210"))'

      # Run tests (but don't exit immediately so we can upload coverage even if tests fail)
      set +e
      julia +$JULIA_VERSION_ENZYME -O0 --color=yes --project --code-coverage=user -e 'using Pkg; Pkg.test(coverage="user")'
      test_status=$$?
      set -e

      # Sanity check: did we actually produce coverage?
      echo "PWD:"; pwd
      echo "Coverage files produced:" `find . -type f -name "*.cov" | wc -l`
      find . -type f -name "*.cov" | head -n 20 || true

      group_slug="{{ matrix.group }}"
      group_slug="$${group_slug//[^A-Za-z0-9_.-]/_}"
      tarball="coverage-${BUILDKITE_BUILD_NUMBER}-${BUILDKITE_JOB_ID}-{{ matrix.architecture }}-$${group_slug}.tgz"

      find . -type f -name "*.cov" > cov_list.txt
      echo "Coverage files found:" `wc -l < cov_list.txt`

      tar -czf "$$tarball" -T cov_list.txt
      ls -lh "$$tarball"

      buildkite-agent artifact upload "$$tarball"

      # Preserve the original test result for Buildkite
      exit "$${test_status}"

    matrix:
      setup:
        architecture:
          - "CPU"
          - "GPU"
        group:
          - "🎭 reactant_2"
          - "👺 enzyme"
          - "👹 reactant_1"

    retry:
      automatic:
        - exit_status: 1
          limit: 1

  #####
  ##### Blocking wait on tests and documentation
  #####

  - wait: ~
    continue_on_failure: true

  #####
  ##### Upload to Codecov
  #####

  - label: "📊 Upload coverage to Codecov"
    agents:
      queue: "Oceananigans-nautilus"
    command: |
      set -e

      mkdir -p coverage_artifacts

      buildkite-agent artifact download "coverage-*.tgz" coverage_artifacts/ || {
        echo "No coverage tarballs were uploaded by test steps."
        exit 0
      }

      # Extract .cov files alongside the real source tree
      for f in coverage_artifacts/coverage-*.tgz; do
        tar -xzf "$$f" -C .
      done

      echo "Found .cov files in checkout:" $$(find . -name "*.cov" | wc -l)
      find . -name "*.cov" | head -n 20 || true

      # Convert Julia .cov -> lcov.info (temp env so we don't edit Project/Manifest)
      julia --color=yes --project -e "import Pkg; Pkg.activate(; temp=true); Pkg.add(\"Coverage\"); using Coverage; cov = vcat(process_folder(\"src\"), process_folder(\"ext\")); LCOV.writefile(\"lcov.info\", cov)"

      echo "Generated lcov.info:"
      ls -lh lcov.info
      test -s lcov.info

      # Upload LCOV to Codecov
      curl -s https://codecov.io/bash | bash -s -- \
        -f lcov.info \
        -F buildkite \
        -F julia

  #####
  ##### Blocking wait on Codecov upload
  #####

  - wait: ~
    continue_on_failure: true

  #####
  ##### Clean up
  #####

  - label: "🧹 clean up environment"
    agents:
      queue: "Oceananigans-nautilus"
    commands:
      - "test -d $OCEANANIGANS_DIR && du -hs $OCEANANIGANS_DIR || true"
      - "test -d $OCEANANIGANS_DIR && rm -rf $OCEANANIGANS_DIR || true"
