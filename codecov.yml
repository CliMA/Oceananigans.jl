coverage:
  range: 50..90
  round: down
  precision: 2

  ignore:
    - "examples"
    - "test"

  status:
    project:
      default:
        target: auto
        threshold: 5%
    patch:
      default:
        target: auto
        threshold: 20%

comment: false

github_checks:
    annotations: false
