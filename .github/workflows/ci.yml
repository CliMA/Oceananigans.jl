name: CI

on:
  pull_request:
    paths:
      - '.github/workflows/ci.yml'
      - 'ext/**'
      - 'src/**'
      - 'test/**'
      - 'Project.toml'
  push:
    branches:
      - '**'
    tags: '*'
    paths:
      - '.github/workflows/ci.yml'
      - 'ext/**'
      - 'src/**'
      - 'test/**'
      - 'Project.toml'

concurrency:
  # Skip intermediate builds: always.
  # Cancel intermediate builds: only if it is a pull request build.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}

jobs:
  reactant:
    name: Reactant - Julia ${{ matrix.version }} - ${{ matrix.os }} - ${{ matrix.arch }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        version:
          - '1.11.9'
        os:
          - ubuntu-latest
        arch:
          - x64
    steps:
      - uses: actions/checkout@v6
      - uses: julia-actions/setup-julia@v2
        with:
          version: ${{ matrix.version }}
          arch: ${{ matrix.arch }}
      - uses: julia-actions/cache@v3
      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
      # - name: Run Reactant tests
      #   shell: bash
      #   run: |
      #     julia --project --color=yes --check-bounds=auto -e '
      #       using Pkg
      #       Pkg.test(; coverage=true,
      #                  julia_args=["--check-bounds=auto", "-O0"],
      #                  force_latest_compatible_version=false,
      #                  allow_reresolve=true)'
      #   env:
      #     TEST_GROUP: "reactant_correctness"
      # - uses: julia-actions/julia-processcoverage@v1
      #   with:
      #     # default is src,ext — include others if you need them
      #     directories: src,ext
